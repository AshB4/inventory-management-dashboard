function App() {
  return (
    <main className="app-shell">
      <section className="hero">
        <p className="eyebrow">Phase 1</p>
        <h1>Inventory Management Dashboard</h1>
        <p className="copy">
          React frontend initialized. Connect this to the Flask backend in the
          next phase.
        </p>
      </section>
    </main>
  );
}

export default App;

